# Hooks System (Codex)

Codex CLI does not support Claude Code hooks (PreToolUse/PostToolUse/Stop).

If you need equivalent behavior, use explicit checklists in skills or run
verifications manually (lint, typecheck, tests) after edits.
