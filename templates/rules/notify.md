# Notify System (Codex)

Codex CLI does not support Claude Code interception lifecycle APIs (PreToolUse/PostToolUse/Stop).

If you need equivalent safeguards, use explicit checklists in skills or run
verifications manually (lint, typecheck, tests) after edits.
